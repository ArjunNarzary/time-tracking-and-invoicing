export const ROUTES = {
  login: "/login",
  register: "/register",
  home: "/",
  dashboard: "/dashboard",
  googleCallback: "/api/auth/callback/google",
}

export const PUBLIC_ROUTES = [
  ROUTES.login,
  ROUTES.register,
  ROUTES.googleCallback,
]
